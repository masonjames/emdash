---
"@emdash-cms/admin": minor
---

Adds a "Published on" field to the content editor sidebar so editors can adjust a published entry's publication date — for example to restore original dates after a migration. Shown to editors and above; the server enforces the existing `content:publish_any` permission on the write.
