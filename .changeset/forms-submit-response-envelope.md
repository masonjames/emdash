---
"@emdash-cms/plugin-forms": patch
---

Fixes enhanced form submissions so standard EmDash plugin API responses wrapped in `{ data }` show the configured success message instead of the generic error state.
